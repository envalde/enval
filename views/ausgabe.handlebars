<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Es wurden folgende Stunden für Mitarbeiter {{ mitarbeiter}} notiert:</h1>
            <table class="table">
                <thead>
                    <tr>
                        <th>Datum</th>
                        <th>Stunden</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>{{ datum }}</td>
                        <td>{{ stunden }}</td>
                    </tr>
                </tbody>
            </table>

        </div>
    </div>
</div>